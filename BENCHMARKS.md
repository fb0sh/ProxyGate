# 性能基线

优化之前先量。这份文件记录**怎么量**和**量到了什么**，所有数字都是本机实测，
不是估算——要质疑某个数就照着下面的命令重跑一遍。

**优化前后的结论**（第 1 项已实施，见"优化后"一节）：池子的读路径改成
`ArcSwap` 快照之后，5,000 条池子上服务端每次发放从 **12.5ms 降到 0.11ms**（113×），
同一个客户端的端到端 QPS 从 634 涨到 2,332（3.7×，现在瓶颈是压测客户端本身）。

优化前测到的原始结论保留在"实测（优化前）"里，它是这次改动的依据：**`/api/v1/get`
的吞吐上限由代理池的写锁决定，而每请求的持锁时间随池子大小线性增长**；同时健康探测
的失败率是 99.1%，即每轮全量重探里 99% 的请求是白花的（第 2 项待做）。

## 怎么量

```bash
# 1. 起一份带池子的服务（见下面的"测量用的配置"）
cargo build --release
PROXYGATE_CONFIG=/path/to/config.yaml ./target/release/proxygate &

# 2. 打 /api/v1/get，同时抓 /metrics 前后差值
python3 scripts/loadtest.py --concurrency 8 --duration 5 --label "..." \
    --pid "$(pgrep -x proxygate | head -1)"
```

`scripts/loadtest.py` 用标准库的线程 + `http.client`；这台机器上没有 wrk/ab/hey，
而它只需要**可复现**，不需要把服务端压到极限。它打印客户端看到的 P50/P95/P99，
以及这一轮里 `proxygate_*` 指标的增量；`--pid` 再补上 fd 数与 RSS。

判断瓶颈看两个地方，**别只看客户端延迟**：

* `proxygate_get_latency_seconds_sum / _count` = 服务端眼里的每次发放耗时；
* `/proc/<pid>/stat` 的 utime+stime 增量 = 服务端真正烧掉的 CPU。

两者差得多，就说明请求在排队（等锁），而不是在算。

## 测量用的配置

`verify: false` 用来把"选择路径"单独拿出来量——否则每次 `/get` 都会现探一个
候选（`selection.max_age`），网络时间会把选择开销完全盖住。

```yaml
server:
  listen: 127.0.0.1:18920
subscribers:
  - name: bulk
    timeout: 20s
    lua_code: |
      -- N 条互不相同的本地代理：端口相同、用户名不同，池子按 URL 去重
      local result = {}
      for i = 1, N do
        table.insert(result, { type = "http", ip = "127.0.0.1", port = 18083, auth = "user" .. i })
      end
      return result
health:
  target: http://127.0.0.1:18082/     # 本地假目标，探测本身很快
  timeout: 2s
  concurrency: 300
selection:
  verify: false
  reuse_after: 30m
state:
  dir: /tmp/pg-e2e/cache-bench
```

## 实测（优化前）

| 场景 | 并发 | QPS | 客户端 P50 | 客户端 P99 | 服务端每次发放 | 服务端 CPU |
| --- | --- | --- | --- | --- | --- | --- |
| 1,000 条池子，选择路径 | 8 | 2,305 | 3.4ms | 8.7ms | 2.8ms | – |
| 5,000 条池子，选择路径 | 8 | 634 | 12.5ms | 15.7ms | 12.5ms | **1.08 核 / 12 核** |
| 1,019 条池子，`verify: true`（判定新鲜） | 4 | 2,360 | 1.6ms | 3.7ms | 1.2ms | – |
| 1,019 条池子，冷启动到 ready | – | – | – | – | – | 15.0s |

启动那次（线上真目标。**注**：这组数字是当时那份示例配置测的，它订阅的是
`rola-ip` + `scdn`；后来 `rola-ip` 因为可用率太低被换成 zdaye 的两个源，下面这些
数字仍然有效——它们是"一份一千来条的池子"的基准，而不是某个具体来源的指标）：

| 指标 | 实测 |
| --- | --- |
| 订阅源脚本 | `scdn` 0.9s / 20 条；`rola-ip` 3.3s / 1,000 条（当时限流到 1,000） |
| 池子 | 1,019 条，**9 条存活**（0.88%） |
| `check_total` | `ok=9`、`fail=1010` → 探测成功率 **0.88%** |
| 冷启动到 ready | **15.0s** |
| 进程 | 13 线程、RSS 92MB、fd 22 |

## 实测（优化后：ArcSwap 快照）

同一批配置、同一台机器、同一个压测脚本，只换了池子的读路径（提交里的
`pool.rs` / `selector.rs`）：

| 场景 | 并发 | QPS | 客户端 P50 | 客户端 P99 | 服务端每次发放 | 服务端 CPU |
| --- | --- | --- | --- | --- | --- | --- |
| 1,000 条池子，选择路径 | 8 | 1,990 | 3.8ms | 9.0ms | **0.034ms** | – |
| 1,000 条池子，选择路径 | 32 | 1,940 | 15.3ms | 38.6ms | **0.036ms** | – |
| 5,000 条池子，选择路径 | 8 | **2,332** | 3.2ms | 7.7ms | **0.110ms** | – |
| 5,000 条池子，选择路径 | 32 | 1,948 | 15.3ms | 37.2ms | 0.109ms | – |
| 5,000 条池子，选择路径 | 64 | 1,935 | 30.3ms | 76.0ms | – | **0.48 核 / 12 核** |

对比：

| | 优化前 | 优化后 |
| --- | --- | --- |
| 5,000 条，多次发放的服务端耗时 | 12.5ms | **0.110ms**（113×） |
| 5,000 条，8 并发的 QPS | 634 | **2,332**（3.7×） |
| 5,000 条，8 并发的客户端 P99 | 15.7ms | **7.7ms** |
| 5,000 条池子跑满时的服务端 CPU | 1.08 核 | 0.48 核（且已不是瓶颈） |
| 1,000 条与 5,000 条的差距 | 2,305 → 634 QPS（随池子大小塌） | 1,990 → 2,332 QPS（不再随池子大小塌） |

**现在的瓶颈是压测客户端，不是服务端。** 证据：32 线程和 64 线程的 QPS 一样
（1,948 / 1,935），只有 P50 在涨（15.3ms → 30.3ms），而服务端只用了 0.48 个核。
换句话说，服务端还有大约一个数量级的余量没被测出来；要接着量得换个不是
Python GIL 的加压工具（`wrk`/`oha`），或者多进程跑这个脚本。

## 这些数字说明了什么（优化前的诊断，保留）

**1. 写锁是 `/get` 的吞吐天花板。** 5,000 条时服务端每次发放 12.5ms，但整轮只
烧了 1.08 个核：12.5ms × 634/s ≈ 7.9 "请求秒/秒"，而 CPU 只有 1.08 核。也就是说
**约 1.7ms 在真正干活（克隆整个池子 + 筛选 + 挑选），其余 10.8ms 在排队等写锁**。
上限 ≈ 1 / 1.7ms ≈ 590 QPS，实测 634 QPS，对得上。池子涨到 10,000 条，持锁时间
再翻一倍，上限就掉到 300 QPS——而且加大并发没有用，只会让 P99 更长（32 并发时
1,000 条池子的 P99 从 8.7ms 涨到 39ms，QPS 反而没涨）。

原因在代码里很直白：`ProxyPool::select()` 持**写锁**，并且为了跑选择器先把整个
池子 `values().cloned()` 复制一遍（`src/pool.rs`）。读多写少的场景用了最重的锁。

**2. 健康探测有 99.1% 是白花的。** 1,019 条里只有 9 条能用，但每轮
`health.interval`（现在 5m）都会把 1,019 条全部重探一遍，而且每条探两个目标：
约 2,000 次请求，其中 99.1% 注定失败。每 5 分钟一次全量重探对死代理毫无意义——
它们不会自己活过来；真正需要频繁重探的只有"已知可用"的那一小撮。

**3. 落盘已经是节流的，但写发生在请求路径上。** `PERSIST_INTERVAL = 2s` 加上
tmp+rename 原子写（`src/state.rs`），一轮 5s 的压力测试里只写了 3 次（指标
`state_save_total` 可以核对）。但那次写是**同步**的 `std::fs::write`，跑在
`/get` 的异步任务里，池子大时会直接堵住 reactor 线程。

### 第 1 项改了什么

* `ProxyPool` 从 `RwLock<HashMap<ProxyId, Proxy>>` 变成
  `ArcSwap<Vec<Arc<Entry>>>` + 一把只服务结构变化的索引锁；
* `Entry` 把可变事实（`alive` / 延迟 / 失败次数 / 判定时间 / 轮次 / 使用时间）
  放进原子量，健康探测写它们时既不拿锁也不重建快照；
* `/get` 与网关只 `load()` 一次共享的 `Arc`，按轮换规则筛候选，再用条目上的
  CAS 认领——一轮内不重复分发的保证从"写锁"换成"CAS"（`tests/selector.rs`
  里 20 线程并发分发的回归测试盯着它）；
* 只有结构变化（合并订阅源结果、裁剪）才重建快照，一轮刷新里每个订阅源一次。

### 第 2、3、5 项改了什么（见下面两节）

* 每个条目带自己的 `next_check_at`：成功按 `health.interval` 排期，失败按
  `backoff_base × 2^n`（上限 `backoff_max`）退避，健康循环按"最近一个到期时刻"
  睡觉而不是固定 tick；
* 落盘从请求线程挪到阻塞线程池（`spawn_blocking`），节流判断仍是原子；
* 新增 `selection.strategy: score` + `selection.sample_size`（默认 32）：采样一批
  候选后按"延迟 + 失败次数 + 闲置时长"打分排序，`random`/`latency` 行为不变。

## 退避重探的实测（第三项）

同一份线上配置（当时是 rola-ip + scdn，真实目标 Google/bing），池子 1,020 条、可用 7~9 条，
`RUST_LOG=proxygate=debug` 跑两分钟，把每一轮的日志时间戳排出来：

| 轮次 | 时刻（相对初次探测） | 探测条数 | 耗时 |
| --- | --- | --- | --- |
| 初次全量 | 0s | 1,020 | 11.5s |
| 到点重探 #1 | +21s | 1,013 | 11.5s |
| 到点重探 #2 | +53s | 1,011 | 11.4s |
| 到点重探 #3 | +104s | 1,009 | 11.5s |

轮次间隔 21s → 32s → 51s，就是 `backoff_base` 的 10s → 20s → 40s 每轮翻倍（每轮的起点
按各自探测完成时刻算，所以还叠了一轮 ~11.5s 的时长）。**每次只有 2~4 条代理能复活**
（1,013 → 1,011 → 1,009），也就是 99% 以上的重探都是确认"它还是死的"。

换算成一小时：旧版固定 12 轮 × 1,020 条 = 12,240 次探测、138s 探测耗时；退避饱和
（30m 上限）之后是约 2 轮 × 1,020 = 2,040 次、23s。**稳态探测开销 -83%**；头几分钟
反而更勤（10s/30s/70s 各一轮），这是有意的：瞬时故障值得多确认几次。

## 全部改完后的 `/get`（三项一起）

同一份 5,000 条池子的配置、同一个压测脚本、8 并发、`verify: false`：

| 策略 | QPS | 客户端 P99 | 服务端每次发放 |
| --- | --- | --- | --- |
| `random` | 1,793 ~ 2,080 | 8.5 ~ 9.9ms | **0.134ms** |
| `score`（采样 32） | 1,843 | 10.3ms | 0.167ms |

和只做第 1 项时的 0.110ms / 2,332 QPS 在同一个量级（差异在压测客户端的噪声范围内，
同一个配置两次跑就有 1,793 与 2,080 的差别）。也就是说第 2、3、5 项没有让发放路径变慢：
`score` 多花的那 0.03ms 是给 32 个候选打分，换来的是"先试更快、更少失败、更近期验证过
的代理"。

## 借代理抓订阅源的开销（`via`）

出站抓取不是发放路径，但借代理会明显变慢，而这条路上的取舍就是"慢一点换不被封"。
同一个 `scdn` 源（一个请求就能拿完、没有分页）实测：

| 出口 | 一轮耗时 | 结果 |
| --- | --- | --- |
| 直连 | 0.35s | found=20 |
| `via: pool`（借到一条健康代理） | 5.0s | found=20 |

0.35s → 5.0s 全部来自免费代理本身（多一次 TLS 握手、节点常在境外、时好时坏），
不是 ProxyGate 的额外开销：抓取走的是同一套 `reqwest` 客户端，只是换了出口。
对 zdaye 这种被 WAF 按 IP 拉黑的站点，这点延迟换来的是"整轮不返回 405"，
所以示例配置里两个 zdaye 源都写了 `via: pool`。池子是空的（冷启动第一次刷新）时
`via: pool` 直接退回直连，第一次刷新不会因此变慢。

## 发放路径随池子大小的扩展性

同一个 harness（本地假上游、`selection.verify: false`、`interval` 与 `backoff_base` 都
调到 30m 让巡检在这一轮里彻底安静），只改池子大小：

| 池子 N | 存活 | 服务端每次发放 | 每次发放的 CPU | 空闲 RSS | QPS（8 客户端） |
| --- | --- | --- | --- | --- | --- |
| 2,000 | 1,464 | 0.055ms | — | 9.9MB | 1,917 |
| 5,000 | 3,632 | 0.092 ~ 0.119ms | 0.23ms | 12.9MB | 1,944 ~ 2,202 |
| 10,000 | 7,191 | 0.210ms | — | 17.6MB | 2,153 |
| 20,000 | 14,329 | 0.402 ~ 0.588ms | 0.56 ~ 0.60ms | 27.2MB | 1,705 ~ 1,937 |

复现方式：池子直接写成 `cache.json`（N 条 `http://userN@127.0.0.1:18083`，指向本地
假上游），每个 N 一个端口和一份 `state.dir`；压测是 `scripts/loadtest.py`，
"每次发放的 CPU" 是跑压测前后读 `/proc/<pid>/stat` 的 utime+stime 差除以发放次数。

两点结论：

* **我们的发放也是 O(N)**：`selector::plan` 每次都要扫一遍 `ArcSwap` 快照，把存活的、
  本轮没用过的、以及"最近没发过"的挑出来。本机的常数是 **20 ~ 40 ns/条**，而且是被
  内存带宽卡住的（20,000 条的工作集约 4MB，超出 L2）：5,000 → 20,000 是 0.09 →
  0.40ms。空闲内存约 **1 KB/条**（8MB 基线 + 20,000 条 → 27MB），只有巡检时客户端缓存
  （上限 512 个 `reqwest::Client`）才会把 RSS 顶到上百 MB。
* **QPS 一列不能当我们的上限**：`/` 这个空接口在同样客户端下也只有 2,187 QPS，
  8 与 16 并发同为 ~2,000，压测脚本本身是瓶颈——这一点和上面"待量"那一行说的是
  同一件事。CPU/次才是可信的数字，20,000 条时 0.56ms/次 ≈ 单核 1,700 次/s。

优化方向因此很清楚：把 `plan()` 的整表扫描换成**增量维护的"存活且本轮未用"索引**，
发放就能在任意大的池子上保持常数成本。现在的 0.4ms 还远没到需要动手的程度。

## 对比 jhao104/proxy_pool（v2.4.0）

对比对象是 <https://github.com/jhao104/proxy_pool>：Python + Flask/gunicorn(4 worker)
+ APScheduler + Redis。**这里没有跑起他们那套栈**（沙箱里没有 Redis，也没有他们的
Python 依赖），所以下面的"他们的数字"是**按代码路径算的模型**：先用
`scripts/pp_cost_model.py` 在本机量出单元成本，再乘上他们代码里的 N。我们的数字是
端到端实测。

### 机制

| 维度 | proxy_pool v2.4.0 | ProxyGate |
| --- | --- | --- |
| 进程 | 采集调度进程 + API 进程 + Redis（≥3 个部件） | 一个二进制，网关与 API 同端口 |
| 来源 | `fetcher/sources/*.py` 插件，扫目录 + mtime 热加载 | 配置里的 Lua 脚本（沙箱、可翻页/签名/自带请求头） |
| 存储 | Redis hash `use_proxy`（外部依赖） | 进程内 `ArcSwap` 快照 + `state.json`/`cache.json` |
| 取一条 | `HKEYS`/`HVALS` 全量 → `random.choice` → `HGET` | 扫快照挑候选 → 轮次 CAS 认领 |
| 轮换 | 均匀随机，**允许连续重复**；`/pop` 不重复但会删掉 | 一轮内不重复、`reuse_after` 内避开、用尽立刻开新轮 |
| 发放前验证 | 无，发的是最近一次巡检结论 | `verify: true`，缓存超过 `max_age`（默认 60s）就先探再发 |
| 巡检 | 每 **2 分钟全量**重探（`HTTP HEAD`，通了再 `HTTPS HEAD`），20 线程 | 每代理 `next_check_at`：成功 5m，失败 5s×2ⁿ 封顶 30m，到点才探 |
| 一次失败 | `MAX_FAIL_COUNT=0` → 直接删 | `max_failures=3` 后进退避 |
| 地域 | 新代理查一次 `api.ip.sb`（每条一次额外请求） | 无 |
| 池子空 | `/get` 返回 `{"code":0,"src":"no proxy"}` | 首次巡检没完成前 503 + `Retry-After` |
| 观测 | `/count` 按来源/https 汇总 | `/metrics`（Prometheus）+ `/api/v1/health` |
| 认证 | 无 | `gateway.auth`（只拦代理请求） |

### 他们的 `/get` 成本（本机单元成本 × N）

`scripts/pp_cost_model.py`，取两次运行的范围：

| N | `/get`（`HKEYS`+parse） | `/get?type=https`（`HVALS`+parse+`json.loads`×N） | 装 hiredis 时 `/get` 的乐观下界 |
| --- | --- | --- | --- |
| 5,000 | 1.5 ~ 1.9ms | 12.5 ~ 14.8ms | 0.5ms |
| 20,000 | 6.2 ~ 8.6ms | 61 ~ 65ms | 1.3ms |
| 50,000 | 17 ~ 20ms | 155 ~ 165ms | 3.7ms |

关键在于**这两条路都是 O(N) 的，而且 O(N) 的部分在 Redis 和 Python 之间**：
每次 `/get` 要把整个池子搬一遍（20,000 条时 `HKEYS` 488KiB、`HVALS` 3.2MiB；50,000 条
时 1.2MiB / 8.1MiB），Redis 单线程要为此阻塞着组织并写出这份响应。4 个同步 worker
一起打，Redis 就是那个串行点。此外巡检每 2 分钟还要 `getCount()` + `getAll()` 各拉
一次全量（`getCount` 内部也是 `HVALS` + 逐条 `json.loads`）：每小时 60 次全量 dump，
5,000 条时每次 12.6ms、合计 0.76 s/h——绝对量不大，但同样是"每次调用 O(N)"。

### 巡检负载（理论 vs 我们的实测）

他们的巡检固定 20 线程、每 2 分钟一轮全量。一轮要发的探测数是
`N`（HTTP）`+ 存活数`（HTTPS）：

| 池子 N | 一轮探测数（按 20% 存活） | 最低探测时长（按每次 1s） | 能否守住 2 分钟 |
| --- | --- | --- | --- |
| 1,000 | 1,200 | 60s | 能 |
| 2,000 | 2,400 | 120s | 刚好卡住 |
| 5,000 | 6,000 | 300s | **不能**，靠任务重叠硬撑 |

`coalesce=False` + `max_instances=10`（`helper/scheduler.py`）意味着跑不完的巡检会
叠起来：最坏情况是 10 份并行任务、200 条探测线程，同时各自还在往 Redis 里拉全量。
N 一过两千，这个"每 2 分钟全量"的承诺就名存实亡。

我们这边同一件事的实测（1,020 条池子）：首轮并发 300 探完 11.5s；之后按 per-proxy
退避重探，稳态约 **2 轮/小时**，对比固定 2 分钟一轮的 30 轮——少了 93% 的探测流量。
代价是死掉的代理最坏要等 `backoff_max`（30m）才知道它回来了，而这一点由发放前
验证兜住：`/get` 只会给出刚刚探过的代理。

### 结论

1. **不发 `/get` 缓存、也不全量重探**：他们用"每 2 分钟全量"换新鲜度，我们用
   "发放前验证 + 每代理退避"换，同样的新鲜度下探测流量差一个数量级。
2. **值得抄的一条：低水位触发抓取。** 他们 `POOL_SIZE_MIN=20`，巡检发现池子太小
   就先抓一轮。我们目前只有 `refresh.interval` 到点或手动 `POST /api/v1/refresh`，
   存活数掉到很低时不会提前补货——低成本，可以加。
3. **不抄的：** `/pop` 的破坏性取用（和我们的非破坏轮换冲突）、每条代理一次地域
   查询（多一个外部依赖，只换来展示字段）、`/get` 无发放前验证（免费池里两分钟前的
   结论基本等于没有）。
4. **顺带印证：** 他们校验请求也带浏览器 UA（`helper/validator.py` 里是 Firefox 34
   的 UA），和我们刚做的改动同一个理由。

## 下一步（按证据排序）

| 优先级 | 改动 | 依据 |
| --- | --- | --- |
| ~~P0~~ | ~~`ArcSwap` 快照 + 运行事实用原子量~~ **已做** | 服务端每次发放 12.5ms → 0.110ms，QPS 634 → 2,332 |
| ~~P0~~ | ~~每代理退避重探~~ **已做** | 稳态探测开销 -83%（上面那张轮次表） |
| ~~P1~~ | ~~落盘挪出请求路径（`spawn_blocking`）~~ **已做** | 同步 `std::fs::write` 不再跑在 `/get` 的异步任务里 |
| ~~P1~~ | ~~采样打分（`strategy: score`）~~ **已做** | 多发 0.03ms/次，换更优的候选顺序 |
| P2 | 低水位触发抓取（存活低于阈值就提前 refresh） | proxy_pool 的 `POOL_SIZE_MIN`，我们现在的补货只跟时间走 |
| P2 | `plan()` 的 O(N) 扫描改成增量索引 | 20,000 条时 0.40ms/次、0.56ms CPU/次，随 N 线性涨 |
| 待量 | 用非 Python 的加压工具重测上限 | 现在 32 与 64 线程同 QPS，`/` 空接口也只有 2,187 QPS，量具先到顶了 |
| 不做 | `/get` 结果缓存 | 和「每次 `/get` 都轮换 + 都是刚验证过的」契约冲突 |
